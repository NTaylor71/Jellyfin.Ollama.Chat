[project]
name = "jellyfin-ollama-chat"
version = "0.1.0"
description = "Jellyfin LLM integration with Django, LangChain, and Ollama"
authors = [{ name = "SGloop", email = "dev@custard.solutions" }]
dependencies = [
    "Django>=5.0",
    "langchain>=0.1.0",
    "psycopg[binary]>=3.1",
    "django-environ",
    "httpx",
    "django-q2",
    "uvicorn",
    "gunicorn"
]
requires-python = ">=3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/webserver"]
